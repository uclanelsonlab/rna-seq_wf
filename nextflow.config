docker.runOptions = '-u $(id -u):$(id -g)'
docker.enabled = true
process.scratch=true

profiles { 
  production {
     process.memory = '50 GB' 
     process.cpus = 10
  }

  testing {
    stubRun = true
    process.memory = '30 GB' 
    process.cpus = 6    
  }
} 